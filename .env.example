# Database Configuration
# On Replit, use DATABASE_URL from secrets. For local development:
DATABASE_URL=postgresql://user:password@localhost:5432/affiliatexchange

# PostgreSQL Connection Details (auto-populated by Replit)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your-password
PGDATABASE=affiliatexchange

# Session Secret (REQUIRED - generate a random string)
SESSION_SECRET=your-random-secret-key-change-this-in-production

# Node Environment
NODE_ENV=development

# Branding (controls various UI labels and Google OAuth warnings)
APP_NAME=AffiliateXchange

# Application Base URL (IMPORTANT for email links)
# Set this to your application's public URL
# Development: http://localhost:5000 (default)
# Production: https://yourdomain.com
# This is used for:
# - Email verification links
# - Password reset links
# - Email notification buttons (payment details, application details, etc.)
# - Google OAuth callbacks
BASE_URL=http://localhost:5000

# Google OAuth Configuration (optional - for Gmail login/signup)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing project
# 2. Enable Google+ API
# 3. Create OAuth 2.0 Client ID credentials
# 4. Add authorized redirect URIs in Google Cloud Console:
#    - Development: http://localhost:3000/api/auth/google/callback
#    - Production: https://yourdomain.com/api/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_CALLBACK_URL can be absolute or relative (defaults to /api/auth/google/callback)
# GOOGLE_CALLBACK_URL=/api/auth/google/callback
# Set GOOGLE_OAUTH_PROMPT=consent after updating OAuth credentials to force Google to refresh cached branding
# GOOGLE_OAUTH_PROMPT=consent

# ============================================
# Social Media OAuth (optional - for creator account connections)
# If not configured, simulated mode will be used for demo purposes
# ============================================

# YouTube OAuth (Google Cloud Console - SEPARATE from Google login OAuth)
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Enable YouTube Data API v3
# 3. Create NEW OAuth 2.0 Client ID (separate from Google login)
# 4. Add redirect URI: http://localhost:3000/api/oauth/youtube/callback
# NOTE: Cannot reuse GOOGLE_CLIENT_ID - different redirect URI required
YOUTUBE_CLIENT_ID=your-youtube-oauth-client-id
YOUTUBE_CLIENT_SECRET=your-youtube-oauth-client-secret

# TikTok OAuth (TikTok Developers Portal)
# 1. Go to https://developers.tiktok.com/
# 2. Create app with Login Kit
# 3. Add redirect URI: http://localhost:3000/api/oauth/tiktok/callback
# 4. Request scopes: user.info.basic, user.info.stats
TIKTOK_CLIENT_KEY=your-tiktok-client-key
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret

# Instagram OAuth (Meta/Facebook Developers)
# 1. Go to https://developers.facebook.com/
# 2. Create Business app
# 3. Add Instagram Graph API product
# 4. Add redirect URI: http://localhost:3000/api/oauth/instagram/callback
# 5. Link Facebook Page to Instagram Business Account
INSTAGRAM_CLIENT_ID=your-instagram-app-id
INSTAGRAM_CLIENT_SECRET=your-instagram-app-secret

# Google Cloud Storage Configuration (REQUIRED for file uploads - videos, images, logos)
# 1. Create a project at https://console.cloud.google.com
# 2. Enable Cloud Storage API
# 3. Create a service account with Storage Object Admin role
# 4. For local development: Download the JSON key file and set GOOGLE_CLOUD_KEYFILE
# 5. For production (Render/Heroku): Set GOOGLE_CLOUD_CREDENTIALS_JSON with the JSON content
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
GOOGLE_CLOUD_BUCKET_NAME=myapp-media-affiliate
# Option 1: Use for local development (path to key file)
GOOGLE_CLOUD_KEYFILE=/path/to/service-account-key.json
# Option 2: Use for production (entire JSON content as a string - best for Render/Heroku)
# GOOGLE_CLOUD_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key":"..."}
GCS_FOLDER=affiliatexchange/videos

# Email Notifications (SendGrid - optional but recommended)
# Sign up at https://sendgrid.com to get an API key
SENDGRID_API_KEY=your-sendgrid-api-key-here
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# Push Notifications (Web Push - optional)
# Generate these using: npx tsx scripts/generate-vapid-keys.ts
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_SUBJECT=mailto:admin@yourdomain.com

# Stripe Payment Processing (optional - only needed for payment features)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key

# Payment Sandbox Mode (optional - for testing payment flows without real transactions)
# Set to 'true' to enable sandbox mode, which simulates successful payments without calling real payment APIs
# This is useful for development and testing when you don't have fully verified payment accounts
PAYMENT_SANDBOX_MODE=false

# Crypto Payment Processing (optional - only needed for cryptocurrency payouts)
# Uses NOWPayments for processing crypto payouts to creators
# Get API credentials from: https://nowpayments.io/
# 1. Create a NOWPayments account at https://nowpayments.io/
# 2. Go to Store Settings > API Keys
# 3. Generate your API key
# 4. For payouts, you need to enable Mass Payouts in your account
NOWPAYMENTS_API_KEY=your-nowpayments-api-key
NOWPAYMENTS_IPN_SECRET=your-ipn-secret
NOWPAYMENTS_IPN_URL=https://yourdomain.com/api/webhooks/nowpayments

# Crypto Sandbox Mode (optional - for testing crypto payment flows)
# Set to 'true' to simulate crypto transactions without real blockchain transactions
# When enabled, payouts will return mock transaction hashes
CRYPTO_SANDBOX_MODE=false

# Note: On Replit, add these to Secrets (Tools → Secrets)
# For local development:
# 1. Copy this file to .env
# 2. Fill in your actual values
# 3. Never commit .env to version control!
